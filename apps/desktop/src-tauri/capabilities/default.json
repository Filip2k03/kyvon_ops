{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for KyvonOPS desktop. Deliberately core-only: command execution reaches a host through the typed, risk-classified SSH path in kyvon-security and kyvon-ssh, never through a local shell capability. Nothing in the frontend used shell:default, and granting it would have opened a second execution path around the approval gate.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
