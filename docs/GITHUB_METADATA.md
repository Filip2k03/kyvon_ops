# GitHub metadata recommendations

These values are recommendations only; no repository metadata is changed by
this document.

## Description

> Local-first DevOps control plane for managing servers through SSH, AI agents, MCP, and policy-controlled operations.

## Homepage

`https://kyvonops.sys.thuyakyaw.com/`

## Topics to review

`devops`, `devops-tools`, `infrastructure`, `server-management`, `ssh`, `rust`,
`tauri`, `sqlite`, `mcp`, `ai-agents`, `automation`, `self-hosted`, `sre`,
`platform-engineering`, `observability`, `desktop-app`

Only add a topic after confirming the repository offers meaningful functionality
for it. Do not use unsupported Kubernetes, cloud, or cybersecurity labels.

## Release link

Use the stable GitHub release link only after a non-draft stable release exists.
Until then, keep the README and website explicit that RC7 is an unsigned draft.
