# KyvonOPS V4.1 Desktop Final Report

Version: `4.1.0` (Cargo workspace, Tauri config, `apps/desktop/package.json`)
Commit: `45bfc71` (desktop release update checks; pushed to `main`)
Release: **draft prerelease only** — `v4.1.0-rc.7` built successfully in GitHub Actions; no stable `v4.1.0` release exists.
Production web: `https://kyvonops.sys.thuyakyaw.com/` (Cloudflare Pages from `main`)

| Artifact | Status | Checksum |
| --- | --- | --- |
| Windows NSIS `.exe` | PARTIAL — RC7 CI artifact built; clean install/launch unverified | `29ef79305902ad80e1b6e3be5eef417d77915ee65a10943af9f302025043f367` |
| macOS `.dmg` (universal) | PARTIAL — RC7 CI artifact built; unsigned, launch/notarization/install unverified | `6acaff48017b47cb99c5c2443b612d6850c3bd132dfb7c76e047cf11d5c84c23` |
| Linux AppImage x86_64 | PARTIAL — RC7 CI artifact built; runtime launch unverified | `b4a943dd4fd0c43492104cce50fdc998d254afc7c88533d00659fafc92402b3e` |
| Linux `.deb` amd64 | PARTIAL — RC7 CI artifact built; install/launch unverified | `48a5d4454d764100ac1e17bedcdaef1bb68c28339f1c60a65da4e73e5ab782f1` |

| Area | Result |
| --- | --- |
| SSH onboarding | PASS (code): password → keychain; absolute key path; host-key UI already present |
| Credential security | PASS (design + code): no show/copy password; vault delete on server delete |
| Server discovery | PARTIAL: `probe_capabilities` + telemetry screens when a session exists |
| Dashboard | PARTIAL: Command Center uses live CPU/memory when collector runs |
| Accessibility | PARTIAL: palette, labels, appearance settings exist; no full WCAG audit |
| CI (push) | PASS for source validation and the desktop release matrix on `v4.1.0-rc.6` |
| GitHub Release | PARTIAL — RC7 draft prerelease contains four installers and `SHA256SUMS.txt`; stable release not published |
| Cloudflare | PASS for public **website**; N/A for desktop binaries |
| Code signing | BLOCKED without org certificates in GitHub secrets |
| Auto-update | BLOCKED / disabled by configuration; desktop now provides a read-only published-release check |

The local macOS artifact was built with Tauri, mounted read-only, and verified
to contain `com.kyvon.ops`, version `4.1.0`, and an arm64 Mach-O executable.
It is unsigned and is not a public release artifact.

## How to produce real installers

```sh
git tag v4.1.0
git push origin v4.1.0
```

The current RC7 draft manifest was downloaded and its four digests match the
GitHub asset digests.
Install and launch each artifact on its target OS, complete signing/notarization,
and only then create the stable `v4.1.0` release.

Do **not** `curl …/install.sh | sh`. Use `scripts/install.sh --desktop` only after
an AppImage + `SHA256SUMS` exist on the release.

## Final

**LAUNCH BLOCKED**
